# Application Configuration
APP_ENV=development
APP_PORT=8080

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER={{.ServiceName}}
DB_PASSWORD=secret
DB_NAME={{.ServiceName}}

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
